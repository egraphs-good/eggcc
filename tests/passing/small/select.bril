# ARGS: 20 30
@main(x: int, y: int) {
  prd: bool = mul x y;
  twenty: int = const 20;
  cmp: bool = lt prd twenty;
  br cmp .then .else;
  .then:
    res: int = id x;
    jmp .done;
  .else:
    res: int = id y;
    jmp .done;
  .done:
    print res;
}