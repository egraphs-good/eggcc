# ARGS: 1 2
@main(x: int, y: int) {
  zero: int = const 0;
  two: int = const 2;
  isless: bool = lt x zero;
  br isless .less .greater;
.less:
  result: int = add x y;
  jmp .done;
.greater:
  xy: int = add x y;
  result: int = mul xy two;
.done:
  print result;
}
