# exerpted from the 'pow' benchmark
@main {
  v0: float = const 3.2;
  x: float = id v0;
  v1: int = const 2;
  n: int = id v1;
  v2: float = id x;
  v3: int = id n;
  call @pow v2 v3;
  v4: int = const 0;
}
